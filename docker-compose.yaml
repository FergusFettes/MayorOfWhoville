version: '3'
services:
  town:
    build: .
    entrypoint: [ "python", "./township.py"]
    network_mode: host
    deploy:
      mode: replicated
      replicas: 5
  cma:
    build: .
    entrypoint: [ "python", "./center_for_mayoral_activities.py"]
    container_name: cma
    network_mode: host
